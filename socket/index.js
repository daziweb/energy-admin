const socketHandle = {
  /**
   * [saveUserSocketId 保存用户的id和socketid][saveUserSocketId 保存用户的id和socketid]
   * @param userid [用户id]
   * @param socketid [用户的socketid]
   */
  saveUserSocketId(userId, socketId) {
    console.log(userId);
    console.log(socketId);
  }
};

module.exports = socketHandle;
